@model IEnumerable<DSWebUI.Models.DogManagerVM>

@{
    ViewData["Title"] = "Index";
}

<h1>List of Dog Store Managers</h1>

<p>
    <a asp-action="Create">Create a New Manager</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <!--@Html.ActionLink("Check", "Edit", new { /* id=item.PrimaryKey */ }) |-->
                <!--@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ })-->
                @Html.ActionLink("Stores", "Index", "StoreLocation", new {id = item.PhoneNumber})
                <!--@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })-->
            </td>
        </tr>
}
    </tbody>
</table>
